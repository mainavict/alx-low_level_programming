#include "main.h"

/**
 * find the squreroot of  n
 * @n is the  number to be squred
 *Return 0;
 */

int _sqrt_recursion(int n)
{
	int i = 1;

	if (n < 0)
	{
		return (-1);
	}
	else
	{
return (1 * (_sqrr_recursion n));
	}}	 
